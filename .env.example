# NovaCRM AI Support Triage — Environment Variables
# Copy this file to .env and fill in your values

# Optional: OpenAI API key for live AI classification + response generation
# Without this, the app uses rule-based fallbacks (full demo still works)
OPENAI_API_KEY=sk-...
